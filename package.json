{
  "name": "tedi-beautify",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "description": "Build tooling for the TEDI Beautify extension. The shipped artifact is the bundled extension.js (built in CI into the release .zip, not committed); source lives in src/.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node --check extension.js && node scripts/verify.mjs",
    "typecheck": "tsc -p jsconfig.json",
    "verify": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.0"
  }
}
